apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp
  namespace: prod
spec:
  ingressClassName: traefik

  tls:
  - hosts:
    - eshop.course-datascientest2026.cloud-ip.cc
    secretName: eshop-tls

  rules:
  - host: eshop.course-datascientest2026.cloud-ip.cc
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webapp
            port:
              number: 80
